{
  "name": "gscindex-vendor-effects",
  "private": true,
  "version": "1.0.0",
  "description": "Build-time only workspace. Vendors two npm-distributed React effect packages (thinking-orbs, metal-fx) into framework-free browser bundles consumed by the Python Dash app's assets/ folder. Nothing here ships to the browser as-is — `npm run build` regenerates assets/vendor/*.js, which ARE committed to git (the Dash app has no client build step in production).",
  "type": "module",
  "scripts": {
    "build": "node scripts/build-thinking-orbs.mjs && node scripts/build-metal-fx.mjs"
  },
  "dependencies": {
    "metal-fx": "^1.0.4",
    "thinking-orbs": "^0.1.1"
  },
  "devDependencies": {
    "esbuild": "^0.28.1",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  }
}
